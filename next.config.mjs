import MiniCssExtractPlugin from "mini-css-extract-plugin";
/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    domains: ["st2.depositphotos.com", "wwww.google.com", ""],
  },
  reactStrictMode: true,
};

export default nextConfig;
